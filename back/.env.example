# Database Configuration
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=university_exam_db
DB_PORT=5432
DB_SCHEMA=public

# Server Configuration
PORT=8080
GO_ENVIRONMENT=development

# Security Configuration (replace with your own secure values)
JWT_SECRET=replace-with-your-secret
CSRF_SECRET=replace-with-your-secret

# Cache Configuration
CACHE_DURATION=60 # in seconds

# Logging Configuration
LOG_LEVEL=debug
LOG_FILE_PATH=logs/app.log

# Additional Settings
TZ=Asia/Tokyo
