# ==========================================
# アプリケーション基本設定
# ==========================================
# 実行環境（development, production, test）
NODE_ENV=development
# タイムゾーン（日本時間）
TZ=Asia/Tokyo
# アプリケーションのポート番号
PORT=8080

# ==========================================
# データベース設定
# ==========================================
# データベースホスト名（Docker Composeのサービス名）
DB_HOST=postgres
# データベースポート番号（PostgreSQLのデフォルトポート）
DB_PORT=5432
# データベース名
DB_NAME=university_exam_db
# データベースユーザー名（PostgreSQLのデフォルトユーザー）
DB_USER=postgres
# データベースパスワード（本番環境では必ず変更してください）
DB_PASSWORD=your_secure_password
# データベーススキーマ名（PostgreSQLのデフォルトスキーマ）
DB_SCHEMA=public

# ==========================================
# API設定
# ==========================================
# フロントエンドからアクセスするAPIのベースURL
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# ==========================================
# セキュリティ設定
# ==========================================
# CSRFトークンの秘密鍵（本番環境では必ず変更してください）
CSRF_SECRET=your_secure_csrf_secret
# JWTトークンの秘密鍵（本番環境では必ず変更してください）
JWT_SECRET=your_secure_jwt_secret

# ==========================================
# 認証設定
# ==========================================
# 認証トークンをローカルストレージに保存する際のキー名
NEXT_PUBLIC_AUTH_TOKEN_KEY=auth_token
# 認証トークンのプレフィックス（Bearer等）
NEXT_PUBLIC_AUTH_TOKEN_PREFIX=Bearer
# 認証トークンの有効期限（秒）
NEXT_PUBLIC_AUTH_TOKEN_EXPIRE=3600

# ==========================================
# キャッシュ設定
# ==========================================
# キャッシュの有効期限（秒）
CACHE_DURATION=60

# ==========================================
# ログ設定
# ==========================================
# ログレベル（debug, info, warn, error）
LOG_LEVEL=debug
# ログファイルのパス
LOG_FILE_PATH=logs/app.log

# ==========================================
# メトリクス設定
# ==========================================
# メトリクス収集の有効/無効
METRICS_ENABLED=true
# メトリクスサーバーのポート番号
METRICS_PORT=9090
# メトリクスエンドポイントのパス
METRICS_PATH=/metrics

# ==========================================
# ヘルスチェック設定
# ==========================================
# ヘルスチェックの有効/無効
HEALTH_ENABLED=true
# ヘルスチェックエンドポイントのパス
HEALTH_PATH=/health
# ヘルスチェックの実行間隔
HEALTH_CHECK_INTERVAL=30s

# ==========================================
# 注意事項
# ==========================================
# 1. このファイルは.env.developmentのテンプレートです
# 2. 実際の開発環境では、このファイルを.env.developmentにコピーして使用してください
# 3. 本番環境では、必ず以下の値を変更してください：
#    - DB_PASSWORD
#    - CSRF_SECRET
#    - JWT_SECRET
# 4. 機密情報は必ず.env.developmentにのみ保存し、Gitにコミットしないでください
# 5. 開発環境では、セキュリティ設定を緩和していますが、本番環境では適切な値を設定してください
# 6. データベースの接続情報は、Docker Composeの設定と一致している必要があります
